You are a senior Front-end Engineer,
The figma designs provided is in multi step.
Firstly I want you to create files for each steps, 
it seems there are four steps so each steps takes a file 
and let us then use a page.tsx as our main file which holds the main JS logics.

Note that there are reusable components in the ui components folder such as buttons and so on.
Import components for menu-bar and side bar, the main work is to design the pages.
Also strictly use the use the colors from the global CSS no arbitrary colors or color codes.


You are a senior Front-end Engineer,
You are to recreate the figma designs provided.
I want you to create a file for each page
and let us then use a page.tsx as our main file which holds the main JS logics and import the files.
We will need to create a dummy data for this page as well no hardcoding.

Note that there are reusable components in the ui components folder such as buttons and so on.
Import components for menu-bar and side bar, the main work is to design the pages don't touch or edit the reusable or imported components.
Also strictly use the use the colors from the global CSS no arbitrary colors or color codes.

